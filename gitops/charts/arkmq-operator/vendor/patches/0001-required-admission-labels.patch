--- a/templates/_helpers.tpl
+++ b/templates/_helpers.tpl
@@ -31,6 +31,18 @@
 {{- end }}
 
 {{/*
+Admission-policy labels supplied by the wrapper chart. Keep these metadata-only:
+adding them to a Deployment selector makes future label changes impossible.
+*/}}
+{{- define "arkmq-org-broker-operator.requiredLabels" -}}
+{{- with .Values.global }}
+{{- with .requiredLabels }}
+{{ toYaml . }}
+{{- end }}
+{{- end }}
+{{- end }}
+
+{{/*
 Common labels
 */}}
 {{- define "arkmq-org-broker-operator.labels" -}}
@@ -40,6 +52,7 @@
 app.kubernetes.io/version: {{ .Chart.AppVersion | quote }}
 {{- end }}
 app.kubernetes.io/managed-by: {{ .Release.Service }}
+{{ include "arkmq-org-broker-operator.requiredLabels" . }}
 {{- end }}
 
 {{/*
--- a/templates/deployment.yaml
+++ b/templates/deployment.yaml
@@ -23,6 +23,7 @@
         control-plane: controller-manager
         name: activemq-artemis-operator
       {{- include "arkmq-org-broker-operator.selectorLabels" . | nindent 8 }}
+      {{- include "arkmq-org-broker-operator.requiredLabels" . | nindent 8 }}
     spec:
       containers:
       - args: {{- toYaml .Values.controllerManager.manager.args | nindent 8 }}
