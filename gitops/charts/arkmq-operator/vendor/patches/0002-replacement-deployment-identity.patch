--- a/templates/deployment.yaml
+++ b/templates/deployment.yaml
@@ -1,7 +1,11 @@
 apiVersion: apps/v1
 kind: Deployment
 metadata:
-  name: activemq-artemis-controller-manager
+  # v2 is a deliberate one-time identity migration. Kubernetes cannot update
+  # the selector on the original Deployment, whose selector shape varied
+  # across earlier chart revisions. Argo creates this replacement, then prunes
+  # the obsolete Deployment without requiring an imperative cluster delete.
+  name: activemq-artemis-controller-manager-v2
   labels:
     control-plane: controller-manager
   {{- include "arkmq-org-broker-operator.labels" . | nindent 4 }}
@@ -17,7 +21,6 @@
     matchLabels:
       control-plane: controller-manager
       name: activemq-artemis-operator
-    {{- include "arkmq-org-broker-operator.selectorLabels" . | nindent 6 }}
   template:
     metadata:
       labels:
