schemaVersion: topology.artemis.apache.org/v2
environment: prod
clusterName: PLACEHOLDER_PROD_EKS_CLUSTER
platformNamespace: artemis-platform

# This is the source of truth consumed directly by Argo CD. Edit Workload Cell
# identity, sizing, features, and enablement here; no generation step is needed.
# Replace placeholder-prod-domain.example.invalid with the full cluster domain
# on the work computer; retain the workloadNamespace subdomain.
workloadCells:
  - workloadCellName: prod-pe
    workloadNamespace: artemis-int-pe
    coordinationId: prod-pe-pair01
    logicalEnvironment: PE
    trafficClass: internal
    managementHost: artemis-int-pe.placeholder-prod-domain.example.invalid
    storageSize: 100Gi
    profile: standard
    resources:
      requests:
        cpu: "1"
        memory: 4Gi
      limits:
        cpu: "2"
        memory: 6Gi
    features:
      diskGuardrail: conservative
    enabled: "false"
  - workloadCellName: prod-pp
    workloadNamespace: artemis-int-pp
    coordinationId: prod-pp-pair01
    logicalEnvironment: PP
    trafficClass: internal
    managementHost: artemis-int-pp.placeholder-prod-domain.example.invalid
    storageSize: 500Gi
    profile: standard
    resources:
      requests:
        cpu: "1"
        memory: 4Gi
      limits:
        cpu: "2"
        memory: 6Gi
    features:
      diskGuardrail: conservative
    enabled: "false"
  - workloadCellName: prod-pp-external
    workloadNamespace: artemis-ext-pp
    coordinationId: prod-pp-ext01
    logicalEnvironment: PP
    trafficClass: external
    managementHost: artemis-ext-pp.placeholder-prod-domain.example.invalid
    storageSize: 500Gi
    profile: standard
    resources:
      requests:
        cpu: "1"
        memory: 4Gi
      limits:
        cpu: "2"
        memory: 6Gi
    features:
      diskGuardrail: conservative
    enabled: "false"
  - workloadCellName: prod-pp-batch
    workloadNamespace: artemis-batch-pp
    coordinationId: prod-pp-batch01
    logicalEnvironment: PP
    trafficClass: batch
    managementHost: artemis-batch-pp.placeholder-prod-domain.example.invalid
    storageSize: 500Gi
    profile: standard
    resources:
      requests:
        cpu: "1"
        memory: 4Gi
      limits:
        cpu: "2"
        memory: 6Gi
    features:
      diskGuardrail: conservative
    enabled: "false"
  - workloadCellName: prod-dm
    workloadNamespace: artemis-int-dm
    coordinationId: prod-dm-pair01
    logicalEnvironment: DM
    trafficClass: internal
    managementHost: artemis-int-dm.placeholder-prod-domain.example.invalid
    storageSize: 100Gi
    profile: standard
    resources:
      requests:
        cpu: "1"
        memory: 4Gi
      limits:
        cpu: "2"
        memory: 6Gi
    features:
      diskGuardrail: conservative
    enabled: "false"
  - workloadCellName: prod-pr
    workloadNamespace: artemis-int-pr
    coordinationId: prod-pr-pair01
    logicalEnvironment: PR
    trafficClass: internal
    managementHost: artemis-int-pr.placeholder-prod-domain.example.invalid
    storageSize: 500Gi
    profile: standard
    resources:
      requests:
        cpu: "1"
        memory: 4Gi
      limits:
        cpu: "2"
        memory: 6Gi
    features:
      diskGuardrail: conservative
    enabled: "false"
  - workloadCellName: prod-pr-external
    workloadNamespace: artemis-ext-pr
    coordinationId: prod-pr-ext01
    logicalEnvironment: PR
    trafficClass: external
    managementHost: artemis-ext-pr.placeholder-prod-domain.example.invalid
    storageSize: 500Gi
    profile: standard
    resources:
      requests:
        cpu: "1"
        memory: 4Gi
      limits:
        cpu: "2"
        memory: 6Gi
    features:
      diskGuardrail: conservative
    enabled: "false"
  - workloadCellName: prod-pr-batch
    workloadNamespace: artemis-batch-pr
    coordinationId: prod-pr-batch01
    logicalEnvironment: PR
    trafficClass: batch
    managementHost: artemis-batch-pr.placeholder-prod-domain.example.invalid
    storageSize: 500Gi
    profile: standard
    resources:
      requests:
        cpu: "1"
        memory: 4Gi
      limits:
        cpu: "2"
        memory: 6Gi
    features:
      diskGuardrail: conservative
    enabled: "false"
