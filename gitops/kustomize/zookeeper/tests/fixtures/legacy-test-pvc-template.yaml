# Immutable PVC template rendered before the Helm chart propagated enterprise
# commonLabels into volumeClaimTemplates (f57de36 through b214e91).
metadata:
  name: data
  labels:
    helm.sh/chart: zookeeper-0.1.0
    app.kubernetes.io/name: zookeeper
    app.kubernetes.io/instance: test-shared-zookeeper
    app.kubernetes.io/version: 3.9.5
    app.kubernetes.io/managed-by: Helm
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: PLACEHOLDER_TEST_GP3_STORAGE_CLASS
  volumeMode: Filesystem
  resources:
    requests:
      storage: 10Gi