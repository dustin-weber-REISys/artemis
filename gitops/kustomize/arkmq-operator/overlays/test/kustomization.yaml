apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

labels:
  - pairs:
      env: test
    includeSelectors: false
    includeTemplates: true

patches:
  - path: private-images.patch.yaml
