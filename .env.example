# Local-only Artemis Compose defaults. Do not use these credentials outside this stack.
ARTEMIS_USER=localdev
ARTEMIS_PASSWORD=localdev

# Optional host-port overrides; the defaults match the documented local endpoints.
# ARTEMIS_OPENWIRE_PORT=61616
# ARTEMIS_AMQP_PORT=5672
# ARTEMIS_STOMP_PORT=61613
# ARTEMIS_MQTT_PORT=1883
# ARTEMIS_HAWTIO_PORT=8161
